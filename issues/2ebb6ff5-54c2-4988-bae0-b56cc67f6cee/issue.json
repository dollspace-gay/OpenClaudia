{
  "uuid": "2ebb6ff5-54c2-4988-bae0-b56cc67f6cee",
  "display_id": 525,
  "title": "Phase 1 spec: tools — file operations (Read/Write/Edit/NotebookEdit/Glob/Grep) [CC parity]",
  "description": "## Goal\n\nExtract a behavioral spec for this subsystem by reading Claude Code's TypeScript source. **Output**: update this issue body with a markdown spec pinning down each testable contract (input → expected output, edge cases, error paths). No Rust yet — that is Phase 2.\n\nParent audit: #523.\n\n## Claude Code source — read these\n\n- `tools/FileReadTool/FileReadTool.ts` (~1183 lines) — read with offset/limit, image-as-base64, PDF passthrough\n- `tools/FileReadTool/imageProcessor.ts` (~94 lines) — PNG/JPEG/GIF/WebP base64 encoder\n- `tools/FileReadTool/limits.ts` (~92 lines) — per-tool token cap + truncation messaging\n- `tools/FileEditTool/FileEditTool.ts` (~625 lines) — string replace, fail on missing old_string\n- `tools/FileEditTool/utils.ts` (~775 lines) — diff helpers\n- `tools/FileWriteTool/FileWriteTool.ts` (~434 lines) — write with parent-dir create\n- `tools/GlobTool/GlobTool.ts` (~198 lines) — glob-pattern file search\n- `tools/GrepTool/GrepTool.ts` (~577 lines) — regex + context lines\n- `tools/NotebookEditTool/NotebookEditTool.ts` (~490 lines) — cell update by `cell_id` with `cell_number` fallback\n\n## OpenClaudia counterpart\n\n- `src/tools/file/read.rs` (~336 lines) — read + offset/limit at line 298, images 40-54\n- `src/tools/file/edit.rs` (~123 lines)\n- `src/tools/file/write.rs` (~75 lines)\n- `src/tools/file/list.rs` (~34 lines)\n- `src/tools/file/notebook.rs` (~304 lines)\n- **missing** — no GlobTool equivalent; file discovery falls through to bash\n- **missing** — no GrepTool; only described in bash tool prompt\n\n## Behaviors to specify\n\nFor each, capture: input shape, expected output, error paths, side effects, and at least one edge case.\n\n1. `read` with `offset` + `limit` returns the correct line slice (compare CC vs OC offset semantics — 0- vs 1-indexed)\n2. `read` on a PNG/JPEG/GIF/WebP returns base64-encoded image content (and the size cap)\n3. `read` on a PDF: CC delegates externally; document OC behavior. Missing binary → actionable error, not panic.\n4. `edit` with `old_string` not found returns an explicit error (not silent no-op)\n5. `edit` with `replace_all=true` replaces every occurrence atomically\n6. `write` creates parent directories when missing\n7. `notebook_edit` `replace` updates the correct cell by `cell_id`; falls back to `cell_number`\n8. File exceeding per-tool token cap surfaces explicit truncation message with offset/limit guidance\n\n## Gaps already visible from the brief\n\nThese are CC features OpenClaudia does not implement. Do **not** include them in the spec — file each as a separate feature/refactor issue and link back here.\n\n- **GlobTool missing** in OC — file as a feature issue: \"Add native glob tool (parity with CC `tools/GlobTool`)\"\n- **GrepTool missing** in OC — file as a feature issue: \"Add native grep tool with regex + context lines (parity with CC `tools/GrepTool`)\"\n\n## Done when\n\n- This issue body has been edited to contain a markdown spec, one section per behavior above\n- Each behavior section names the function(s) on both sides (or marks the OC side missing)\n- Every gap above has been filed as its own issue and linked here\n- The matching Phase 2 issue is unblocked\n",
  "status": "open",
  "priority": "high",
  "parent_uuid": "59f5166e-466b-48d4-bc9a-668b2ed08aee",
  "created_by": "lOrm",
  "created_at": "2026-05-18T15:29:14.768378003Z",
  "updated_at": "2026-05-18T15:29:20.984389232Z",
  "labels": [
    "audit",
    "parity",
    "phase-1-spec",
    "cc-tools-fileops"
  ]
}