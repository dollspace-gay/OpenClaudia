{
  "uuid": "cf26e35d-8fee-4d86-ae80-9025a0d6338f",
  "issue_uuid": "4aba17c7-1b3c-411b-b388-591e7001ab3c",
  "author": "lOrm",
  "content": "## Current state (src/tui/components/, ~757 LOC)\n\n**Plumbing landed in b9e13aa:**\n- `components/mod.rs`: `Overlay` trait (`render(frame, area)` + `handle_key(key) -> OverlayAction`), `OverlayAction::{Consumed|Close|ResumeSession(String)}`, `centered_rect(percent_x, percent_y, area)` helper.\n- `components/help.rs` (351 LOC): scrollable keyboard-shortcut overlay.\n- `components/log_selector.rs` (303 LOC): JSONL session picker with date/preview.\n- `tui/app.rs:420-423`: `enum ActiveOverlay { Help(HelpOverlay), LogSelector(LogSelector) }` — adding a variant requires the match arms at app.rs:911-912 (key dispatch) and app.rs:1780-1781 (render).\n- `keybindings/resolver.rs:20`: `KeyAction::ModelPicker` already exists as a binding target but no overlay listens.\n\n**Not landed:** ModelPicker, ModePicker, NotificationBar, AskUserQuestion dialog.\n\n## Phase 2 spec — four components, each ~250 LOC\n\n**1. ModelPicker — `components/model_picker.rs`**\n```rust\npub struct ModelPicker {\n    models: Vec<ModelEntry>,\n    selected: usize,\n    scroll_offset: usize,\n}\npub struct ModelEntry {\n    pub provider: String,        // \"anthropic\"\n    pub model_id: String,         // \"claude-sonnet-4-5\"\n    pub display_name: String,     // \"Claude Sonnet 4.5\"\n    pub supports_thinking: bool,\n    pub default_effort: EffortLevel,\n}\nimpl ModelPicker {\n    pub fn from_config(config: &AppConfig) -> Self;  // iterates config.providers\n}\nimpl Overlay for ModelPicker { /* Up/Down nav, Enter → returns OverlayAction::Selected(ModelEntry) */ }\n```\nExtend `OverlayAction` with `SelectModel(ModelEntry)`. App handles it by updating `App::model` + `App::provider` + the next outgoing request's endpoint headers. Open via `/model` (no args) — wire in `cli/commands/mod.rs` slash dispatch.\n\n**2. ModePicker — `components/mode_picker.rs`** (mirror of ModelPicker)\n```rust\npub struct ModePicker { modes: &'static [(Mode, &'static str)], selected: usize }\n```\nModes are the existing `Mode::{Build|Plan|Extend|Refactor}` enum from `tui/app.rs`. `OverlayAction::SelectMode(Mode)`. Open via `/mode` (no args).\n\n**3. NotificationBar — `components/notification_bar.rs`** (NOT an overlay)\nRender directly in `tui/app.rs:1780`-adjacent code as a one-row strip at the bottom of `frame.size()`, ABOVE the input box, when `App::notifications.front().is_some()`.\n```rust\npub struct Notification {\n    pub text: String,\n    pub kind: NotificationKind,   // RateLimit | ToolDenied | MemorySaved | Info\n    pub created_at: Instant,\n    pub ttl: Duration,            // default 3s\n}\npub struct NotificationBar { queue: VecDeque<Notification> }\nimpl NotificationBar {\n    pub fn push(&mut self, n: Notification);\n    pub fn tick(&mut self);                    // drop expired\n    pub fn current(&self) -> Option<&Notification>;\n    pub fn render(&self, frame: &mut Frame, area: Rect);\n}\n```\nPush sites: rate-limit response in `proxy.rs`, permission denial in `permission_mgr`, memory.rs `archival_insert`. Add `App::notifications: NotificationBar` field, call `notifications.tick()` in the main event loop before render.\n\n**4. AskUserQuestion dialog — `components/ask_user_question.rs`**\nIntercepts the existing `USER_QUESTION_MARKER` JSON payload (src/tools/mod.rs:113, src/tools/ask_user.rs:151). Current flow surfaces it as plain text — replace that with overlay open.\n```rust\npub struct AskUserQuestionOverlay {\n    questions: Vec<Question>,\n    current_q_idx: usize,\n    cursor: usize,\n    selections: Vec<Vec<usize>>,  // per-question multi-select\n}\npub struct Question {\n    pub prompt: String,\n    pub options: Vec<String>,\n    pub multi_select: bool,\n}\n```\nDetection point: `tools::execute_tool` returns a string whose first byte is `{` — parse with `serde_json::from_str::<UserQuestionPayload>`; if it matches, route the result to the App via a new channel `App::pending_question_tx` instead of streaming as a message. Overlay submits via `OverlayAction::AnswerQuestions(Vec<Vec<usize>>)`, App formats the response and feeds it back to the proxy as the tool result.\n\n**File layout:**\n- 4 new files under `src/tui/components/` (~250 LOC each)\n- 4 new `ActiveOverlay` variants + their match arms (3 sites × 4 = 12 lines)\n- 4 new `OverlayAction` variants\n- 2 slash-command dispatch entries (`/model`, `/mode`)\n- 1 notification-bar render call\n\n**Test scaffold** — per component:\n- `<component>_navigates_with_arrow_keys`\n- `<component>_returns_action_on_enter` / `<component>_closes_on_esc`\n- For NotificationBar: `expired_notifications_dropped_on_tick`, `queue_caps_at_8`.\n\nEach component is a self-contained PR. Closing the umbrella — Phase 2 can land them in any order.",
  "created_at": "2026-05-20T17:52:26.296867559Z",
  "kind": "result",
  "signed_by": "SHA256:eHpbrpU/tOUroPHQwwGZTQKY+0tU0E2rZw5zFEs1jHE",
  "signature": "U1NIU0lHAAAAAQAAADMAAAALc3NoLWVkMjU1MTkAAAAgqj5+UR1olg7w0+zZwuWbsWpzM2FlVsxso1l+rn8dSfAAAAARY3Jvc3NsaW5rLWNvbW1lbnQAAAAAAAAABnNoYTUxMgAAAFMAAAALc3NoLWVkMjU1MTkAAABAip/hIVErwAxCKWlQXDGIMF1QhXcqL9TucK8dRoCSbITKk8QS35BxWzx3mr8hTsZcTGoZqUm8n1GvaubbVCQtCQ=="
}