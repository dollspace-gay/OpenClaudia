{
  "uuid": "76a92a30-86be-4734-b061-ba6dfed2edb6",
  "display_id": 786,
  "title": "rules.rs duplicates extension/language registry across three constants",
  "description": "## Severity\nMinor\n## Dimension\ndry\n## Location\nsrc/rules.rs:11-56 (extension_to_language), 147-208 (parse_rule_name, known_languages)\n## Violation\nThe mapping between source extensions and language names is hard-coded in TWO independent constants that must stay in sync:\n\n```rust\nfn extension_to_language(ext: &str) -> Option<&'static str> {\n    match ext.to_lowercase().as_str() {\n        \"rs\" => Some(\"rust\"),\n        ...\n}\n...\nlet known_languages = [\n    \"rust\", \"python\", \"javascript\", ...\n];\n```\n\nPlus a third partial copy in `auto_learn.rs::has_source_extension` / `has_file_extension` (hard-coded `\"rs\"|\"py\"|\"ts\"|\"js\"`). Adding a new language requires three coordinated edits or the rules engine silently degrades. The two arrays disagree today on at least `\"sql\"` and `\"shell\"` (in `known_languages` but with a different ext→lang mapping in `extension_to_language` — `sql` maps to `sql`, `sh` maps to `shell`).\n\nThe string→language match also uses `.to_lowercase()` (allocates a `String` on every lookup) instead of `.eq_ignore_ascii_case()` (no alloc).\n## Mandated refactor\nDefine ONE `const LANGUAGE_REGISTRY: &[(&str, &[&str])]` mapping a canonical language name to its known extensions. Derive `extension_to_language` and `known_languages` as lazily-built `HashMap` / `HashSet` from this single source. Make `auto_learn.rs` consume the same registry via `pub use rules::is_source_extension`. Remove the `to_lowercase` allocation in favor of byte-wise case-insensitive comparison.\n## Filed from QA review dispatch",
  "status": "closed",
  "priority": "medium",
  "created_by": "lOrm",
  "created_at": "2026-05-18T21:57:49.085925799Z",
  "updated_at": "2026-05-20T07:22:36.574121782Z",
  "closed_at": "2026-05-20T07:22:36.574121782Z"
}